settings:
  env:
    APP_URL: https://example.com
    API_KEY: my-api-key
    TEST_DATA_DIR: /home/user/LoadScan/data

# For reporting to Blazemeter (optional, requires an account).
modules:
  blazemeter:
    token: my-key-id:my-key-secret
