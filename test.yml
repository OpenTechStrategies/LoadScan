included-configs:
- secrets.yml

settings:
  artifacts-dir: artifacts/%Y-%m-%d_%H-%M-%S.%f

execution:
- concurrency: 2
  ramp-up: 1m
  hold-for: 1m
  scenario: login

scenarios:
  login:
    requests:
    - url: ${APP_URL}/app/login
      method: GET
      label: login-page
